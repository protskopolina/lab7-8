<Window x:Class="LabWPF_5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWPF_5.ViewModels"
        mc:Ignorable="d"
        Title="Student Management MVVM (LabWPF_5)" Height="450" Width="600">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <ListBox x:Name="StudentsList" 
                     ItemsSource="{Binding Students}" 
                     SelectedItem="{Binding SelectedStudent}"
                     HorizontalContentAlignment="Stretch"
                     AlternationCount="2" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                <Setter Property="Background" Value="#f0f0f0"/>
                            </Trigger>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#ffffff"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding FullName}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Age, StringFormat={}{0} років}" Margin="10,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="У списку немає елементів" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="Gray"
                       FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Students.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Прізвище"/>
            <TextBox Text="{Binding EditLastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Ім'я"/>
            <TextBox Text="{Binding EditFirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Вік (16-100)"/>
            <TextBox Text="{Binding EditAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Стать"/>
            <ComboBox SelectedValue="{Binding EditGender}" SelectedValuePath="Content" Margin="0,0,0,20">
                <ComboBoxItem Content="Чол"/>
                <ComboBoxItem Content="Жін"/>
            </ComboBox>

            <Button Content="Додати" Command="{Binding AddCommand}" Margin="0,0,0,5"/>
            <Button Content="Зберегти зміни" Command="{Binding EditCommand}" Margin="0,0,0,5"/>
            <Button Content="Видалити" Command="{Binding DeleteCommand}" Background="#FFDDDD"/>
        </StackPanel>
    </Grid>
</Window>